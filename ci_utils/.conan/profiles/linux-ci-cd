include(default)

[settings]
os=Linux
arch=x86_64
compiler=gcc
compiler.cppstd=23
# compiler.version=12
# compiler.libcxx=libstdc++11
# compiler.musl=1.2

[options]
# boost 1.91 CCI recipe declares cobalt_io_ssl as expected-built but
# never produces it; package_info() raises in the standalone deps build
# where the consumer conanfile's options aren't applied. kth doesn't
# use cobalt.
boost/*:without_cobalt=True
